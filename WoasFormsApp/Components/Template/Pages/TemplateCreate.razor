@page "/Template/Create"
@rendermode InteractiveAuto

@attribute [Authorize]

<PageTitle>Template Editor - WoasForms</PageTitle>

<MudText Typo="Typo.h3" Align="Align.Center">Creating New Template</MudText>
<TemplateEditor editorMode="TemplateEditor.EditorMode.Create" templateModel="@template" @rendermode="InteractiveAuto"/>

@code
{
    Template template = new Template() { Fields = new List<TemplateField>() };
}